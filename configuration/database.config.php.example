<?php
    return array(
        // ------------------------------------------------------------
        // Data Source Name
        // ------------------------------------------------------------
        'DSN' => 'mysql:localhost;dbname=liquiderp',

        // ------------------------------------------------------------
        // The database authentication username, if no username is
        // set, enter the value null
        // ------------------------------------------------------------
        'username' => 'liquiderp',

        // ------------------------------------------------------------
        // The database authentication password, if no password is
        // set, enter the value null
        // ------------------------------------------------------------
        'password' => '',

        // ------------------------------------------------------------
        // Database driver arguments
        // ------------------------------------------------------------
        'driverArguments' => array(
            //PDO::MYSQL_ATTR_INIT_COMMAND => 'SET NAMES utf8'
        )
    );
